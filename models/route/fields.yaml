# ===================================
#  Field Definitions
# ===================================

fields:
    route:
        label: Route
        span: left
        type: dropdown
        comment: The route address. IE. /account/login
        options: getPossibleRoutes
    enabled:
        label: Enabled
        span: right
        type: switch
        comment: Enable the Route Restrictions
        default: true