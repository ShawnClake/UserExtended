# ===================================
#  Field Definitions
# ===================================

fields:
    route:
        label: Route
        span: left
        type: text
        comment: The route address. IE. /account/login
    enabled:
        label: Enabled
        span: right
        type: switch
        comment: Enable the Route Restrictions
        default: true