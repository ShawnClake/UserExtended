# ===================================
#  Column Definitions
# ===================================

columns:
    route:
        label: Route
        searchable: true

    description:
        label: Descrption
        searchable: true

    enabled:
        label: Enabled
        type: switch

    cascade:
        label: Cascades
        type: switch

    attempts:
        label: Access Attempts
        type: number

    last_accessed_at:
        label: Last Access Attempt
        type: datetime

    created_at:
        label: rainlab.user::lang.group.created_at
        type: datetime