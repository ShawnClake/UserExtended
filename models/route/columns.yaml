# ===================================
#  Column Definitions
# ===================================

columns:
    route:
        label: Route
        searchable: true

    enabled:
        label: Enabled
        type: switch

    attempts:
        label: Times Accessed
        type: number

    last_accessed:
        label: Last Acessed
        type: datetime

    created_at:
        label: rainlab.user::lang.group.created_at
        type: datetime